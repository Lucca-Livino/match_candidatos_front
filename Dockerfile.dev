FROM node:22

WORKDIR /usr/src/app

COPY package*.json ./

RUN npm ci

COPY . .

EXPOSE 5173

# --host expoe o dev server em 0.0.0.0: sem isso o Vite escuta so no loopback
# do container e a porta publicada nao responde no host.
CMD ["npm", "run", "dev", "--", "--host"]
